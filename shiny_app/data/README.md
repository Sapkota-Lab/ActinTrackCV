# Local Shiny Data

Keep small demo CSV files here only if they are safe to commit.

Do not store raw microscopy files or generated videos in this folder. The Shiny app is designed to point at the existing ActinTrackCV project workspace instead.
